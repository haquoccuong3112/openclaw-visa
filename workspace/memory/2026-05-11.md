# 2026-05-11 - Dong Hanh Bot Session

## Những gì đã làm

### 1. Refactor pipeline OCR (`run_sop_v2.py`)
- Bỏ ghi tab Documents + Activity Log trên Sheet
- Đổi output `.txt` → `.md` human-readable
- File `.md` + `.json` upload vào `_Bot OCR & Metadata` trên Drive
- Code: `/home/cuong/scan-ho-so/run_sop_v2.py`

### 2. Build Telegram Listener (`telegram_listener.py`)
- Bot: @donghanhprocessingbot (token lưu trong `scan-ocr.env`)
- Code: `/home/cuong/scan-ho-so/telegram_listener.py`
- Service: `/etc/systemd/system/donghanhbot.service`
- Registry: `/home/cuong/scan-ho-so/group_registry.json`

### 3. Logic tự động khi bot join 2 nhóm
**Parse tên nhóm:**
- KH: `Hoàng Thị Mơ TEST7 1991 WP10m - C Liên` → kind=kh, applicant, visa
- Pro: `DH Pro WP10m - Hoàng Thị Mơ TEST7 1991` → kind=pro
- Kết nối cặp KH↔Pro bằng pair_key = `applicant|visa`

**Khi cả 2 nhóm join:**
1. Tạo Drive folder `{applicant} {visa}` trong OpenClaw folder
2. Tạo subfolder: Personal Docs, Education, Asset, Employment, _Bot OCR & Metadata
3. Detect nhân viên trong nhóm Pro (cần bot là admin)
4. Share Drive folder cho nhân viên (email từ Staff tab)
5. Tạo/cập nhật Sheet cá nhân cho từng nhân viên
6. Gửi thông báo vào nhóm Pro + nhóm KH

**Khi nhóm KH gửi file:**
- Im lặng trong nhóm KH
- OCR (Document AI) → Gemini classify → rename SOP
- Upload Drive đúng subfolder
- Forward file (đã rename) + caption vào nhóm Pro
- Xóa file tạm

### 4. Cấu hình đã setup
- Service account: `scan-ho-so-bot@ally-visa-bot.iam.gserviceaccount.com`
- Nâng quyền lên organizer trên Shared Drive Bot folder
- Staff list: 13 người (3 Director, 3 Manager, 7 Staff) - lưu tab Staff trên master Sheet
- Master Sheet: `1Qv4gdxNKgS7EsDPvInFsR1rnob_Qgv9HCaays_al6io`
- Shared Drive ID: `0AIYOQpLqtMPvUk9PVA`
- OpenClaw folder: `1VUpoBV3fAudONv5mMFXYguRThKfOLyz7`

### 5. Test case
- Nhóm KH: `-1003937104742` (Hoàng Thị Mơ TEST7 1991 WP10m - C Liên)
- Nhóm Pro: `-1003511393720` (DH Pro WP10m - Hoàng Thị Mơ TEST7 1991)
- Drive folder: `1QW_qwtAClCo-mHD7qaFx-IxMpU1khLBq`
- Staff detected: Hà Quốc Cường, Bảo Ngân, Huy Hoàng

## Vấn đề còn lại
- Bot nhận được file từ nhóm KH (confirmed trong log) nhưng chưa chạy OCR xong
- Handler thứ tự đã fix (handle_file trước debug_all)
- Cần test lại end-to-end: gửi file → OCR → rename → Drive → forward Pro
- Bot đang STOPPED, cần `sudo systemctl start donghanhbot` để chạy lại

## Lệnh hữu ích
```bash
sudo systemctl start donghanhbot      # khởi động
sudo systemctl stop donghanhbot       # dừng
sudo systemctl restart donghanhbot    # restart
sudo journalctl -u donghanhbot -f     # xem log realtime
cat /home/cuong/scan-ho-so/group_registry.json  # xem registry
```
# 2026-05-11 - Dong Hanh Bot Session

## Những gì đã làm

### 1. Refactor pipeline OCR (`run_sop_v2.py`)
- Bỏ ghi tab Documents + Activity Log trên Sheet
- Đổi output `.txt` → `.md` human-readable
- File `.md` + `.json` upload vào `_Bot OCR & Metadata` trên Drive
- Code: `/home/cuong/scan-ho-so/run_sop_v2.py`

### 2. Build Telegram Listener (`telegram_listener.py`)
- Bot: @donghanhprocessingbot (token lưu trong `scan-ocr.env`)
- Code: `/home/cuong/scan-ho-so/telegram_listener.py`
- Service: `/etc/systemd/system/donghanhbot.service`
- Registry: `/home/cuong/scan-ho-so/group_registry.json`

### 3. Logic tự động khi bot join 2 nhóm
**Parse tên nhóm:**
- KH: `Hoàng Thị Mơ TEST7 1991 WP10m - C Liên` → kind=kh, applicant, visa
- Pro: `DH Pro WP10m - Hoàng Thị Mơ TEST7 1991` → kind=pro
- Kết nối cặp KH↔Pro bằng pair_key = `applicant|visa`

**Khi cả 2 nhóm join:**
1. Tạo Drive folder `{applicant} {visa}` trong OpenClaw folder
2. Tạo subfolder: Personal Docs, Education, Asset, Employment, _Bot OCR & Metadata
3. Detect nhân viên trong nhóm Pro (cần bot là admin)
4. Share Drive folder cho nhân viên (email từ Staff tab)
5. Tạo/cập nhật Sheet cá nhân cho từng nhân viên
6. Gửi thông báo vào nhóm Pro + nhóm KH

**Khi nhóm KH gửi file:**
- Im lặng trong nhóm KH
- OCR (Document AI) → Gemini classify → rename SOP
- Upload Drive đúng subfolder
- Forward file (đã rename) + caption vào nhóm Pro
- Xóa file tạm

### 4. Cấu hình đã setup
- Service account: `scan-ho-so-bot@ally-visa-bot.iam.gserviceaccount.com`
- Nâng quyền lên organizer trên Shared Drive Bot folder
- Staff list: 13 người (3 Director, 3 Manager, 7 Staff) - lưu tab Staff trên master Sheet
- Master Sheet: `1Qv4gdxNKgS7EsDPvInFsR1rnob_Qgv9HCaays_al6io`
- Shared Drive ID: `0AIYOQpLqtMPvUk9PVA`
- OpenClaw folder: `1VUpoBV3fAudONv5mMFXYguRThKfOLyz7`

### 5. Test case
- Nhóm KH: `-1003937104742` (Hoàng Thị Mơ TEST7 1991 WP10m - C Liên)
- Nhóm Pro: `-1003511393720` (DH Pro WP10m - Hoàng Thị Mơ TEST7 1991)
- Drive folder: `1QW_qwtAClCo-mHD7qaFx-IxMpU1khLBq`
- Staff detected: Hà Quốc Cường, Bảo Ngân, Huy Hoàng

## Vấn đề còn lại
- Bot nhận được file từ nhóm KH (confirmed trong log) nhưng chưa chạy OCR xong
- Handler thứ tự đã fix (handle_file trước debug_all)
- Cần test lại end-to-end: gửi file → OCR → rename → Drive → forward Pro
- Bot đang STOPPED, cần `sudo systemctl start donghanhbot` để chạy lại

## Lệnh hữu ích
```bash
sudo systemctl start donghanhbot      # khởi động
sudo systemctl stop donghanhbot       # dừng
sudo systemctl restart donghanhbot    # restart
sudo journalctl -u donghanhbot -f     # xem log realtime
cat /home/cuong/scan-ho-so/group_registry.json  # xem registry
```

---

## Session update sau compaction flush (2026-05-11 chiều)
- Cường yêu cầu tiếp tục triển khai/test `@donghanhprocessingbot` cho nhiều nhóm test Hoàng Thị Mơ TEST1-8.
- Bot service: `donghanhbot.service`, code chính vẫn là `/home/cuong/scan-ho-so/telegram_listener.py`.
- Pipeline đã đổi từ Document AI OCR sang Gemini-only: file PDF/JPG/PNG gửi trực tiếp qua OpenRouter model `google/gemini-2.5-flash-lite`; Document AI không còn là luồng chính.
- Hỗ trợ zip: giải nén zip, bỏ qua file rác macOS `._*` và `__MACOSX/*`; xử lý file hợp lệ trong zip song song.
- Tối ưu tốc độ: Telegram concurrent updates tăng cao, Gemini concurrency từng đặt 6, batch file concurrency 8, concurrent updates 16. Cần cẩn trọng rate-limit nếu lỗi 429.
- Luồng gửi Pro đã đổi nhiều lần; trạng thái cuối theo yêu cầu Cường: không gửi file/media group nữa; chỉ gửi summary cực ngắn vào nhóm Pro theo format:
  `✅ Upload xong N file` + dòng trống + danh sách đánh số tên file mới + link Drive folder.
- Khi bot setup xong trong nhóm khách hàng chỉ nhắn đúng icon `✅`.
- Tên Sheet nhân viên đổi format: `Đồng Hành - {Tên nhân viên}`.
- Staff/manager sheets được tạo bằng cách copy template mẫu `1S0kr9nBuTJHLbTwa_O8XfYVvaadviVyamUBiUKMwWJM`; không append xuống cuối template nữa mà upsert vào dòng trống đầu tiên dựa trên identity columns.
- File tổng hợp private dùng sheet ID `1bPNea4i86yVqwTGx0IuKGv1t3PrDlKoPAg16yPNOnCs`; cập nhật tất cả case vào tab `Cases`, có cột `MANAGER` và `STAFF`, không share file tổng hợp cho nhân viên.
- Quyền Drive + Sheet phụ trách hiện dựa vào thành viên trong **nhóm khách hàng**, không dựa vào nhóm `DH Pro`. Nhóm Pro chỉ để nhận summary kết quả.
- Director (Cường, Telegram ID `5359705508`) luôn được thêm/cập nhật Drive/Sheet dù Telegram không detect được trong nhóm.
- Cường xác nhận Telegram ID của họ: `5359705508`.
- Logic auto revoke quyền khi remove nhân viên khỏi nhóm đã thử nhưng Telegram không gửi event ổn định; theo yêu cầu Cường đã bỏ logic revoke/remove handler. Không tự thu hồi quyền khi remove member nữa.
- Đã gặp race condition khi add nhiều nhóm nhanh: `concurrent_updates(16)` khiến `load_registry -> save_registry` đè nhau, làm mất KH TEST2/TEST4. Đã thêm `REGISTRY_LOCK` quanh phần join/setup registry; sau đó backfill setup TEST2/TEST4. Cần giữ lock để add nhiều nhóm cùng lúc không mất registry.
- Có đề xuất chưa chắc đã implement đầy đủ: atomic save registry + auto reconcile registry để chống miss event tốt hơn. Nếu tiếp tục harden, ưu tiên hai mục này.
- Cường đã nhiều lần yêu cầu chỉ check log không can thiệp. Cuối phiên đang yêu cầu check TEST1-8 upload 4 zip xử lý thế nào, chỉ check không sửa.
- Tình trạng được báo gần cuối: TEST1-8 đang xử lý nhiều zip song song; đã thấy xong summary cho `hoang thi mo3.zip` (3 file, fail 0), `hoang thi mo1.zip` (3 file, fail 0), `hoang thi mo4.zip` (5 file, fail 0); chưa xác nhận toàn bộ xong.
- Các rule classify đã bổ sung: thẻ tín dụng/credit card/Visa/Mastercard -> `The Visa-MC` trong `Personal Docs`; từng có ảnh Techcombank bị ra `Khac` trước khi thêm rule.
- Dọn test trước đó: Cường yêu cầu xoá khách test trong Sheets và trash folder Drive test; đã báo đã xoá Master/file tổng hợp/staff sheets và trash folder TEST/TEST2/TEST3/TEST4/TEST5/TEST7/TEST8, reset registry `{}`.
